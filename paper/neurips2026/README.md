# Overleaf draft

Upload the contents of this directory to a new Overleaf project and set `main.tex` as the main document. The source uses the official NeurIPS 2026 style with the `dblblindworkshop` option and the workshop title `Transitioning from Pre-Training to Post-Training`.

Before submission:

1. Compile and inspect page count, figure legibility, and overfull boxes.
2. Confirm whether the workshop requires anonymous review. If it does not, replace `Anonymous Author(s)` and use the workshop option requested by the organizers.
3. Confirm funding, conflicts, author contributions, and the AI-use statement. The workshop call does not request the main-track checklist, so `main.tex` does not include it.
4. Keep references and the appendix outside the 4 to 5 page main-text budget if the submission system follows the workshop call.
